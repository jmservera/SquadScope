# Documentation session — 2026-09-21

## Scope

Audited every file changed by merged PR `jmservera/SquadScope#770`, its stored PR narrative, and nearby W38 reliability artifacts for incorrect W38/W39 incident scope.

## Correction

W39 is the sole missed-publication incident. W38 ultimately published successfully after manual recovery reached Azure and is retained only as comparative/recovery-path evidence.

## State model

- Attempt records are immutable facts and remain non-green when blocked, failed, rejected, partial, or unknown, even after recovery.
- Weekly identity is derived separately.
- `published_verified` requires exact identity-bound provider readback proving `provider=published` and `external_verified=true`.
- `published_verified_recovered` requires the same exact readback plus preserved earlier non-green attempt evidence.
- Manual action, workflow success, API acceptance, partial stages, unknown outcome, duplicate ambiguity, and missing readback remain non-green.
- W39 remains non-green because no dispatch/provider readback occurred. W38 remains the successful comparative case, but its failed automatic attempt is not rewritten; the machine weekly state requires exact provider readback rather than inference from the recovery workflow.

## Validation status

- Podcast dispatch regression suite: `203 passed`.
- `git diff --check`: passed.
- Ten affected repository/session Markdown files: no tabs or trailing whitespace.
- Prohibited-claim search: no false W38 missed-publication, W38/W39 joint-incident, or five-consecutive-total-failures claim.
- Repository Markdown lint command: unavailable; no repository Markdown lint configuration or script was found.
- Hosted CI: pending and not represented as locally passed.
